package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Primary & Neutral Brand Tokens (Pure White Background & Black Text)
val BrandPrimary = Color(0xFF000000)        // Pure Black
val BrandPrimaryLight = Color(0xFF1F2937)   // Deep Charcoal
val BrandPrimaryContainer = Color(0xFFF3F4F6) // Crisp Light Neutral
val BrandOnPrimaryContainer = Color(0xFF000000)

val BrandSecondary = Color(0xFF111827)      // Jet Black
val BrandSecondaryContainer = Color(0xFFF3F4F6)
val BrandOnSecondaryContainer = Color(0xFF000000)

val BrandTertiary = Color(0xFF1F2937)
val BrandTertiaryContainer = Color(0xFFF3F4F6)

// Semantic Academic Risk & Mastery Tokens (PRESERVED FOR STATUS: RED & GREEN & AMBER)
val RiskHigh = Color(0xFFDC2626)            // Status: Crimson Red
val RiskHighContainer = Color(0xFFFEE2E2)
val RiskMedium = Color(0xFFD97706)          // Status: Amber Orange
val RiskMediumContainer = Color(0xFFFEF3C7)
val RiskLow = Color(0xFF059669)             // Status: Emerald Green
val RiskLowContainer = Color(0xFFD1FAE5)

val MasteryNotAssessed = Color(0xFF6B7280)  // Neutral Grey
val MasteryNotAssessedContainer = Color(0xFFF3F4F6)

// Canvas & Surface Tokens (White Background, Black Text)
val LightBackground = Color(0xFFFFFFFF)     // Pure White
val LightSurface = Color(0xFFFFFFFF)        // Pure White
val LightSurfaceVariant = Color(0xFFE5E7EB) // Visible light gray track and divider
val LightTextPrimary = Color(0xFF000000)    // Pure Black
val LightTextSecondary = Color(0xFF1F2937)  // High-contrast near-black
val LightTextMuted = Color(0xFF4B5563)      // Clear readable dark slate
val LightBorder = Color(0xFFD1D5DB)         // Crisp visible border line (Tailwind Gray 300)

// Dark Palette Tokens (Aligned with White Background & Black Text)
val DarkBackground = Color(0xFFFFFFFF)
val DarkSurface = Color(0xFFFFFFFF)
val DarkSurfaceVariant = Color(0xFFE5E7EB)
val DarkTextPrimary = Color(0xFF000000)
val DarkTextSecondary = Color(0xFF1F2937)
val DarkBorder = Color(0xFFD1D5DB)

